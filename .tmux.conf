set -g default-terminal "screen-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

# set -g prefix C-a
# unbind C-b
# bind-key C-a send-prefix

set -g base-index 1
set -g pane-base-index 1

set-option -g focus-events on

unbind %
bind | split-window -h

unbind '"'
bind - split-window -v

unbind r
bind r source-file ~/.tmux.conf

bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5

bind -r m resize-pane -Z

# bind M-c attach-session -c "#{pane_current_path}"

set -g mouse on

set -wg automatic-rename on
set -g automatic-rename-format "Window"

# set-window-option -g mode-keys vi

# bind-key -T copy-mode-vi 'v' send -X begin-selection # start selecting text with 'v'
# bind-key -T copy-mode-vi 'y' send -X copy-selection # copy with 'y'

# unbind -T copy-mode-vi MouseDragEnd1Pane # don't exit copy mode when dragging with mouse

# tpm plugin
set -g @plugin 'tmux-plugins/tpm'

#--------- PLUGINS
set -g @plugin 'christoomey/vim-tmux-navigator'
# set -g @plugin 'tmux-plugins/tmux-resurrect' # persist tmux sessions after computer restart
# set -g @plugin 'tmux-plugins/tmux-continuum' # automatically saves sessions for you every 15 minutes
set -g @plugin 'tmux-plugins/tmux-battery'
# set -g @plugin 'tmux-plugins/tmux-clima'
set -g @plugin 'theo64oliver/tmux-code-time'
set -g @plugin 'olimorris/tmux-pomodoro-plus'

run-shell ~/.tmux/plugins/tmux-code-time/code-time.tmux

# --------- THEMES
set -g @batt_icon_charge_tier8 '󰁹'
set -g @batt_icon_charge_tier7 '󰂂'
set -g @batt_icon_charge_tier6 '󰂀'
set -g @batt_icon_charge_tier5 '󰁿'
set -g @batt_icon_charge_tier4 '󰁾'
set -g @batt_icon_charge_tier3 '󰁽'
set -g @batt_icon_charge_tier2 '󰁻'
set -g @batt_icon_charge_tier1 '󰁺'
set -g @batt_icon_status_charged '󰂄'
set -g @batt_icon_status_charging '󱐌'
set -g @batt_icon_status_discharging '󱟟'

# source-file ~/.tmux/themes/colors/nordic.conf
source-file ~/.tmux/themes/colors/tokyo-night.conf
# source-file ~/.tmux/themes/colors/catppuccin.conf
# source-file ~/.tmux/themes/minimal.conf
# source-file ~/.tmux/themes/traditional.conf
# source-file ~/.tmux/themes/slash_minimal.conf
source-file ~/.tmux/themes/neovim.conf

# ---- Catppuccin preset
# Configure Catppuccin
# set -g @catppuccin_flavor "mocha"
# set -g @catppuccin_status_background "none"
# set -g @catppuccin_window_status_style "none"
# set -g @catppuccin_pane_status_enabled "off"
# set -g @catppuccin_pane_border_status "off"
#
# # status left look and feel
# set -g status-left-length 100
# set -g status-left ""
# set -ga status-left "#{?client_prefix,#{#[bg=#{@thm_red},fg=#{@thm_bg},bold]  #S },#{#[bg=#{@thm_bg},fg=#{@thm_green}]  #S }}"
# set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_overlay_0},none]│"
# set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_maroon}]"
# set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_overlay_0},none]│"
# set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_blue}]  #{=/-32/...:#{s|$USER|~|:#{b:pane_current_path}}} "
# set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_overlay_0},none]#{?window_zoomed_flag,│,}"
# set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_yellow}]#{?window_zoomed_flag,  ,}"
#
# # status right look and feel
# set -g status-right-length 100
# set -g status-right ""
# set -ga status-right "#{?#{e|>=:10,#{battery_percentage}},#{#[bg=#{@thm_red},fg=#{@thm_bg}]},#{#[bg=#{@thm_bg},fg=#{@thm_pink}]}} #{battery_icon} #{battery_percentage} "
# set -ga status-right "#[bg=#{@thm_bg},fg=#{@thm_overlay_0}, none]│"
# # set -ga status-right "#[bg=#{@thm_bg}]#{?#{==:#{online_status},ok},#[fg=#{@thm_mauve}] 󰖩 on ,#[fg=#{@thm_red},bold]#[reverse] 󰖪 off }"
# # set -ga status-right "#[bg=#{@thm_bg},fg=#{@thm_overlay_0}, none]│"
# set -ga status-right "#[bg=#{@thm_bg},fg=#{@thm_blue}] 󰭦 %Y-%m-%d 󰅐 %H:%M "
#
# # Configure Tmux
# set -g status-position bottom
# set -g status-style "bg=#{@thm_bg}"
# set -g status-justify "absolute-centre"
#
# # pane border look and feel
# # setw -g pane-border-status top
# # setw -g pane-border-format ""
# setw -g pane-active-border-style "bg=#{@thm_bg},fg=#{@thm_overlay_0}"
# setw -g pane-border-style "bg=#{@thm_bg},fg=#{@thm_surface_0}"
# # setw -g pane-border-lines single
#
# # window look and feel
# set -wg automatic-rename on
# set -g automatic-rename-format "Window"
#
# set -g window-status-format " #I#{?#{!=:#{window_name},Window},: #W,} "
# set -g window-status-style "bg=#{@thm_bg},fg=#{@thm_rosewater}"
# set -g window-status-last-style "bg=#{@thm_bg},fg=#{@thm_peach}"
# set -g window-status-activity-style "bg=#{@thm_red},fg=#{@thm_bg}"
# set -g window-status-bell-style "bg=#{@thm_red},fg=#{@thm_bg},bold"
# # set -gF window-status-separator "#[bg=#{@thm_bg},fg=#{@thm_overlay_0}]│"
#
# set -g window-status-current-format " #I#{?#{!=:#{window_name},Window},: #W,} "
# set -g window-status-current-style "bg=#{@thm_peach},fg=#{@thm_bg},bold"

# ----------
# set -g @resurrect-capture-pane-contents 'on'
# set -g @continuum-restore 'on'

# NOTE: Init tmux plugin manager (KEEP THIS AT VERY BOTTOM)
run '~/.tmux/plugins/tpm/tpm'
